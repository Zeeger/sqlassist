using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace SQLAssist.Classes
{
	class SQLDataTableTranslator
	{

		public SQLDataTable SetDataTable(string input)
		{ 
		/*
		 * 
		 * Instantiate a module-level sql parser and use it to write table definitions to the return.
		 * 
		 * Make another class for concatenating SQLDataTables into statements
		 * */
		}
	}
}
